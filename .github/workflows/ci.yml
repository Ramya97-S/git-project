name: CI/CD Pipeline

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build-and-deploy:
    runs-on: self-hosted   # IMPORTANT: your Ubuntu runner

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Run Tests
        run: |
          echo "Running tests..."
          # Add your test commands here
          # e.g., npm test, mvn test, pytest, etc.

      - name: Build Project
        run: |
          echo "Building project..."
          # Replace with your build commands
          # e.g., npm run build, mvn package

      - name: Deploy (Example)
        run: |
          echo "Deploying..."
          # Add your deployment commands
          # Example: copy build files, restart service, deploy to folder, etc.

